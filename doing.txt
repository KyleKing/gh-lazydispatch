2026-07-26 copier freshening follow-ups

- git.DefaultBranches() is now exported (was _defaultBranches) so app and git share the fallback list; rename fine to revert if you prefer it private
- goconst fixes added several small package constants (exec gh* args, chain inputsSegment, demo environmentInput/stagingEnv, panes justNowLabel/statusUnknown); flag any you want named differently

2026-07-27 copier freshening follow-ups

- every release v1.0.1 through v1.0.4 has zero binaries attached, so `gh extension install` and the Homebrew formula are both broken today; the v0.6.2 inline-goreleaser fix should cover it, but the next `fix:` or `feat:` on main is the first real proof
- consider backfilling binaries for v1.0.4 rather than waiting, since users hitting install today get nothing
- dependabot PRs #10 (go deps) and #14 (action pins) are both red on lint against a stale base; #14 also touches the now-deleted release.yml, so it needs a rebase or a close
- `hk fix --all` exits 1 on pre-existing shellcheck SC2086 info findings in .github/workflows/version-bump.yml; CI does not run it that way, so nothing is failing, but the repo cannot go green on an all-files run until those are quoted
