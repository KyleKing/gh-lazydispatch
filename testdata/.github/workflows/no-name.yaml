on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Message to print'
        type: string
        default: 'Hello World'

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ inputs.message }}"
